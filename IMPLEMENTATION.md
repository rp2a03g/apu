# 実装メモ

このツールがどの順番で何を作ってきたかの記録。フェーズごとに、追加したファイルと
そのとき決めたことを残してある。使い方は [README.md](README.md)、設計上の不変条件は
[DESIGN.md](DESIGN.md)、音程まわりの詳細は [DESIGN-PITCH.md](DESIGN-PITCH.md) を参照。

読むためのものなので、ここに書いてある日付や経緯は当時のもの。現在の仕様は
コードとMMLコマンドヘルプが正本。

---

## 現在の状態: Phase 1 + Phase 2 + Phase 3 + Phase 4 + Phase 5 + Phase 6

### Phase 1: 6502アセンブラ & NSFジェネレーター

- `src/asm/opcodes.js` — 6502 命令セット（合法151オペコード）の定義
- `src/asm/assembler.js` — 2パス方式の簡易6502アセンブラ
  - ラベル、各種アドレッシングモード、`.org` `.byte`/`.db` `.word`/`.dw` `.res`/`.ds` ディレクティブに対応
- `src/nsf/nsfHeader.js` — NSF 128バイトヘッダの生成・解析。**NSFe(チャンク形式のNSF拡張、2026-09-07)**も読める: `MML.NSF.normalize(bytes)` が NSF/NSFe どちらでも「128バイトヘッダ+DATA」の素のNSFバイト列と `header` を返し、再生/キャプチャ/変換/Worker/ヘッドレスは従来のNSF経路をそのまま通す(INFO/DATA/BANK/RATE/NSF2 をヘッダへ詰め、auth の全文(31文字制限なし、UTF-8→Shift-JIS→Latin-1 の順で判別)を `header.songName` 等へ)。NSFヘッダに載らない曲ラベル(tlbl/taut)・演奏時間(time/fade)・再生順(plst)・text/regn/mixe/VRC7 は `header.nsfe` に別枠で入り、NSFパネルのヘッダ情報と鍵盤表示タイトル行の曲一覧に出す。演奏時間は曲が変わったときに再生時間欄へ反映(SPCのID666と同じ扱い)、再生順があれば曲送り/自動送りはその並びで進む。未知の大文字(必須)チャンクはエラー、小文字は無視。検証データ: `emu sound/famicompo/Famicompo/`(44本、全て INFO/time/fade/auth/DATA、1本だけBANK)
- `src/nsf/nsfBuilder.js` — ヘッダ＋プログラムイメージからNSFバイナリを生成、ダウンロード

### Phase 2: NSF対応エミュレータ（6502 CPU + 2A03音源）

- `src/emulator/cpu6502.js` — 6502 CPUコア（全151オペコード、`call()`でINIT/PLAYサブルーチン実行）
- `src/emulator/apu2a03.js` — 2A03 APU（パルスx2・三角波・ノイズ・DMCの5ch合成、NES非線形ミキサー実装）
- `src/emulator/nsfBus.js` — NSF実行用メモリバス（$4000-$4017をAPUへルーティング、バンクスイッチ対応）
- `src/emulator/nsfPlayer.js` — INIT/PLAY呼び出し + フレーム単位の音声サンプル生成
- `index.html` / `src/main.js` — 各パネルの組み立てと、アセンブル結果からエミュレータ再生までの配線

### Phase 3: 再生ログ一括キャプチャ & シーク/早送り/巻き戻し

- `src/emulator/capture.js`
  - `MML.Emu.captureSong(nsfBytes, opt)` — INIT実行後、指定秒数分のPLAYルーチンを毎フレーム実行し、全フレームの音声波形（DCブロック済み）とレジスタ書き込みタイムラインログを一括生成
  - `MML.Emu.dcBlock(samples)` — APU出力のDCオフセットを除去するワンポールDCブロッカー
- `index.html` / `src/main.js`
  - 「再生・シーク（一括キャプチャ）」パネルを追加（キャプチャ長さ指定、キャプチャ実行ボタン）
  - キャプチャ結果を `AudioBuffer` 化し、`AudioBufferSourceNode.start(0, offset)` による即時シークを実現
  - 再生・一時停止・停止・巻き戻し(-5秒)・早送り(+5秒)・シークバーによるトランスポートUIを実装

### Phase 4: MMLコンパイラ（ppmck互換 基本コマンド）

- `src/mml/lexer.js` — ソースをチャンネル(A-D)別のコマンド文字列に分割し、トークン化
- `src/mml/compiler.js` — `MML.Mml.compile(source)`
  - 対応コマンド: `cdefgab`（音符, `+`/`#`/`-`で半音, 数値で音長, `.`で付点）, `r`（休符）, `n<num>[,<len>]`（直接音程指定,オクターブ2のCを0とした通し番号）, `o` `>` `<`（オクターブ）, `l`（デフォルト音長）, `v`（音量0-15。FDSとVRC6のこぎり波だけはppmck同様0-63のレジスタ生値で、FDSは実効32で頭打ち・VRC6サウは実質42が最大＝43以上は実機の8bit桁溢れで鋸波が崩れるだけで音量は上がらない。`@v`/`@vr`のテーブル値も同じ範囲）, `v+` `v-`（音量相対増減,省略時±1）, `q<n>[,<m>]`（ゲートタイム0-8+フレーム加減。鳴る長さはppmckc `calcGateTime` と同じ floor(音長×n/8)+m。2026-09-08まで四捨五入だったが実機は切り捨て）, `@q<n>`（フレーム単位の早期ノートオフ。ppmckc `_QUONTIZE2` = `q8,-n`）, `k<len>`（キーオフ=直前の音符をゲートオフして`<len>`ぶん待つ、ppmck「リリースエンベロープが発動する休符」）, `@k<n>`（**本ツール独自拡張**: キーオンから`<n>`フレームでキーオフ、q/@qより優先、`@k0`で解除。固定オン長で鳴らすドライバの曲を音符長=キーオン間隔のまま1コマンドで書ける。2026-09-08）, `t`（テンポ、曲中の任意位置で変更可）, `K<n>`（移調・半音）, `@`（音色=デューティ比）, `&`（タイ）, `[ ]n`（繰り返し）, `[ ... | ... ]n`（最後の周回だけ`|`以降を演奏しない）, `{ ... }<len>`（連符・等分）, `EN<n>`/`ENOF`+`@EN<n>={...}`（ノートエンベロープ=高速アルペジオ、2A03全ch+全拡張音源(VRC7含む)対応、仕様通り厳密実装。ただし`|`無しのテーブルの末尾はppmck(末尾の値を足し続ける)と違い合計値で止まる）, `EP<n>[,<delay>]`/`EPOF`+`@EP<n>={...}`（ピッチエンベロープ、2A03全ch+VRC6/MMC5/SUNSOFT 5B/FDS/N163対応(VRC7はfnum/blockの対数表現のため対象外)、実機`sound_pitch_enverope`と同一の生レジスタオフセット空間で厳密実装。値の向きは`D<n>`と同じく全音源で正=音程が上がる(2026-09-14統一。ppmckの`#PITCH-CORRECTION`相当を常時適用した向きで、周期レジスタ系チップではNSF書き出し時に減算へ変換)。テーブル値はENと同じ「前フレームからの差分の累積」(実機はレジスタ現在値へ毎フレーム加算する。`|`無しは末尾の値を足し続けるので末尾は0で止める。2026-09-13に「各フレームの絶対値」だった誤実装を再生/NSF書き出し/変換の全層で修正)。`,<delay>`は**ppmck仕様には無いこのツール独自の拡張**＝発動を`<delay>`フレーム遅らせてから0番地からテーブルを辿る。省略時delay=0で従来通り）, `MP<n>`/`MPOF`+`@MP<n>={delay,speed,depth}`（ビブラート、対応範囲はEPと同じ、実機`lfo_sub`/`warizan_start`を忠実移植した厳密実装。滑らかな三角波ではなく実機通りの階段状の変化になる）, `PT<target>,<duration>[,<delay>]`/`PTOF`（ポルタメント=単調な直線グライド、対応範囲はEPと同じ。**ppmckドキュメント（doc/mck.txt）には専用コマンドが無く「ピッチエンベロープ(EP)で代用してください」と明記されているため、これはppmck方言からの独自拡張**。targetはD<n>/EP<n>と同じ生レジスタオフセット空間(正=音程が上がる)、durationフレームで0からtargetへ到達し以降は最終値を保持。内部アルゴリズムはMPの`warizan_start`片道版(反転無し)と同一で6502ドライバ側もCEILDIVを共有）, `s<speed>,<depth>`（スイープ、対応範囲はEPと同じ、実機の生ハードウェアスイープレジスタ書き込みを厳密実装）
  - `@t<len>,<num>`（テンポ2、フレーム単位。`<len>`の音符が確実に`<num>`フレームになる非整数テンポを内部で逆算し、`t<n>`の整数BPM丸めによるフレーム端数化を避ける）, `w<len>`（ウェイト、直前のコマンド=音符/休符/wを音長ぶんそのまま延長。タイと同じ仕組み）, `y<adr>,<num>`（レジスタ直接書き込み、`$`接頭辞の16進アドレス対応。ブラウザ再生でも該当アドレスへそのまま書き込む）, `x<param0>,<param1>`（データストリームへのバイト直接埋め込み。**NSF書き出し=6502バイトコード専用のコマンドで、ブラウザ再生には対応する概念が無いためパースのみ行い無視する**）
  - `SD<n>`/`SDOF`/`SDQR`（セルフディレイ=疑似エコー。リリースエンベロープが発動する区間のピッチを`<n>`個前のノートオンへ差し替え、実機同様その位置で打ち直す。SDQRはノートオン履歴のリセット。ppmck公式リファレンスの出力例と完全一致することを実測確認済み）。**発動には`@vr<n>`（リリースエンベロープ）と`q<n>`（n<8、ゲートを切ってリリース区間を作る）の両方が必要**で、既定の`q8`ではリリース区間自体が存在しないため何も起きない（実機ppmckも同じ）。`<n>`は0〜8（実機`SELF_DELAY_MAX`）、`SD255`は`SDOF`と同義、三角波とDPCMチャンネルでは使用不可（いずれも実機ppmckcの`datamake.c`に準拠）
  - `@<n>={...}` + `@@<n>`（デューティ=音色エンベロープ。`@v<n>`の音色版で、`{ }`内は0-7の値の列、`|`でループ位置を指定できる。1フレーム1ステップでデューティ比が変化する）。対応はデューティ比を持つチップ（2A03パルスA/B・VRC6パルス・MMC5パルス）。波形/音色番号を持つチップ（FDS・N163・VRC7）では`@<n>`と同じ音色（波形）選択として働き、VRC7のみ`@@<64+n>`=`OP<n>`（`@OP<n>`のユーザー音色をロード+音色番号0）の別名も使える。三角波・ノイズ・DPCM・SUNSOFT 5B・VRC6のこぎり波では使用不可（実機ppmckcの対応トラックマスク準拠）。実機同様、`@<n>`を書くとデューティエンベロープは解除される
  - `@@r<n>`（リリース音色、255=OFF。`@vr`の音色版で、ゲートオフの瞬間に音色を`<n>`へ差し替える。デューティ系チップではデューティエンベロープ番号、FDS/VRC7では波形/音色番号として解釈される）。**N163のみ未対応**（波形の共有RAM配置が音符単位のため。書くとコンパイルエラーで通知する）
  - `@vr<n>`（リリースエンベロープ。ゲートオフ=`q<n>`(n<8)や`@q<n>`で音が切れた瞬間から`<n>`番のテーブルで音量を推移させる）。`@v<n>`（ソフトウェア音量エンベロープ）の有無に関わらず発動し、`v<n>`固定音量の音符でもリリースが鳴る（実機ppmckの`putReleaseEffect`が`@v`の有無を条件にしていないのに合わせた仕様）。テーブルは本ツール独自の`@vr<n>={...}`定義を優先し、無ければ**ppmck同様に`@v<n>`定義を参照する**（`@v1={...}`を定義して`@vr1`で呼ぶppmckのMMLがそのまま通る）
  - `SM`/`SMOF`（スムース、対応範囲はEPと同じ(VRC7除く)。上位バイト=周期/周波数レジスタの位相リセットを伴う書き込みを「値が変化した時だけ」に切り替え、同オクターブ内のレガートでクリック音が出るのを防ぐだけの機能で、ピッチ自体はグライドしない。実機`CMD_SMOOTH`/`EFF2_SMOOTH_ENABLE`を実測確認）
  - `PS`（ポルタメント、対応範囲はEPと同じ(VRC7除く)。`c PS g`のように次の音符そのものをグライド先として使う。実機`process_ps`/`pitchshift_setup`のb_div方式ステップ計算を移植した独自実装で、既存の`PT<target>,<duration>`（明示的な生レジスタオフセット指定）とは別物・共存する。★実機は最初の1サイクルだけ間隔にstepでなくdurationそのものを使うためグライドが次の音符へ食い込むことがある独特の挙動があるが、本実装は音符の時間内で目標へ収束する分かりやすい近似を採用している=ppmck公式リファレンスが明記する「PSコマンド後の音程は正確ではない」という注記の対象範囲を意図的に簡略化）
  - `!`（データスキップ。この記号以降、そのチャンネルのMMLは一切コンパイルされない=以降無音）, `!!`（タイムシフト=再生開始位置。シークバーの開始ハンドルと連動）, `!!!`（**ppmckには無いこのツール独自の拡張**＝再生終了位置。シークバーの終了ハンドルと連動、省略時は曲の最後まで）。`compile()`の戻り値`startMarkerFrame`/`endMarkerFrame`で参照できる
  - `@OP<n>={8バイト,$XX可}` / `@OT<n>={TL,FB,...}`（VRC7カスタム音色。8バイト生形式とMGSDRV互換形式の両方に対応、内部的には同じ音色テーブルに統合）, `@FM<n>={64値}`（FDS波形メモリ）, `@N<n>={buf,...}`（N163波形、4の倍数の可変長へ`src/mml/n163Alloc.js`の共有バッファアロケータで格納。複数の異なる波形を同時に確保できる）, `@MW<n>={32値}`+`@MH<n>={delay,freq,depth,waveform}`+`MH<n>`/`MHOF`（FDSピッチ変調。`@MW`には変調カウンタへの増減量そのものを書き、使える値は`0`(維持)/`1`/`2`/`4`/`-1`/`-2`/`-4`/`R`(カウンタを0にリセット)の8種類だけ＝実機の3bitテーブルコードへの読み替えは不要で、それ以外の値はコンパイルエラーになる）, `S<n>`/`M<n>`/`N<n>`（SUNSOFT 5Bハードウェアエンベロープ形状/周期・ノイズ周波数、実機PSGレジスタ仕様通り厳密実装）
  - VRC7/FDSは「音色・波形スロットが1系統のみで全ch共有」という実機の制約はあるが、**曲中の動的な切り替えに対応**: VRC7は`OP<n>`コマンド（出現位置で即座にカスタム音色を再ロード）、FDSは`@<n>`（音色番号指定コマンド）で選択中の`@FM<n>`が変わるたびに自動的に波形を再ロードする。N163は`src/mml/n163Alloc.js`の共有バッファアロケータにより、複数の異なる波形を128byte内蔵RAM上の別領域へ同時に確保できる（VRC7/FDSのような単一スロット共有ではない）
  - 複数行にまたがる`{ ... }`定義ブロック（コメント入り可）に対応
  - `#TITLE`/`#COMPOSER`/`#MAKER`/`#PROGRAMER`（メタ情報、`compile()`戻り値の`meta`に格納。`#TITLE`/`#COMPOSER`/`#MAKER`はNSF書き出し時にヘッダのsongName/artist/copyrightへ反映される。`#PROGRAMER`に対応するNSFヘッダ項目は無く未使用）, `#OCTAVE-REV`（`>``<`反転）, `#GATE-DENOM`（`q<n>`のゲート分母変更）, `#EX-VRC6`等（拡張音源宣言、UIのチェックボックスと統合されMML本文だけで有効化できる）, `#AUTO-BANKSWITCH`等バンキング系・`#INCLUDE`（未対応・認識のみで無視）, `$<char> <mml>`（マクロ定義、1回だけの非再帰展開）
  - `@DPCM<n>={"file",freq,size,dac,mode}`（DMCサンプル定義）はパース済み。1個でも定義があると"E"チャンネル(DPCM)が自動的に有効になり（`#EX-*`宣言不要）、実際の再生は**ppmck準拠**（2026-09-19）: Eチャンネルの音符/`n<num>`が「どの`@DPCM<num>`を鳴らすか」の番号（0〜63）で、再生レートは定義の`freq`で固定。音名で書くとppmckcと同じ「オクターブ×16+音名番号」（`o0 c`=0、`o1 c`=16）になる。`@`/`v`/`@v`/`@vr`/`D`/`EP`/`EN`/`MP`/`K`/`SD`はEではエラー。同じファイル名の定義はROMを共有。参照ファイルの読み込みはDPCMコンバータ（定義のダブルクリックで該当行が開く）で行い、`compile()`の`opt.dpcmSamples[filename]=Uint8Array`にブラウザで読み込んだ変換済みバイト列を渡すと実際に鳴る
  - **MML→NSF書き出し（バンク切り替え・拡張音源対応）**: 「MML作曲」パネルの「NSF書き出し」ボタンで、実際に再生可能な`.nsf`ファイルをダウンロードできる。MMLはppmck方式のコンパクトなバイトコード（音符1個が基本2〜3バイト、`src/nsf/mckBytecode.js`）にシリアライズされ、専用の6502ドライバ（`src/driver/ppmckDriver.js`、実機ppmckの設計を参考に新規実装）が解釈・再生する。NSFバンク切り替え（`$5FF8`-`$5FFF`、4KB窓×8）に対応しており、1チャンネルが4KBを超える曲データも複数バンクにまたがって正しく再生できる。2A03(A-D)に加えVRC6・MMC5・SUNSOFT 5B・FDS・N163・VRC7の全拡張音源に対応（`MML.Mml.compile()`の正解データと突き合わせて実測確認済み）。**使用しているチップのハンドラ・波形/周期テーブルのみを条件付きで固定領域に埋め込む**設計のため、2A03のみの曲では未使用チップのデータは一切含まれない（固定領域実測: 2A03のみ5160B、全6チップ使用でも8431B）。FDS `@FM`・N163 `@N`・VRC7 `@OP`/`@OT`+`OP<n>`のカスタム波形・音色も、実際に定義されている場合のみテーブル・再ロード処理を条件付きで埋め込んで再生できる（定義が無ければ従来通り既定波形/ROMプリセットのみ）。`@t`/`w`/`y`/`SM`/`SMOF`/`PS`/`SD`/`SDOF`/`SDQR`/`@vr`（リリースエンベロープ）/`@@`（デューティエンベロープ）/`@@r`（リリース音色）/`EN`/`EP`/`MP`/`PT`/`s`（ハードウェアスイープ）もNSF書き出しに対応（`@@`は音色バイトのbit7で固定音色と区別する実機同型のエンコードで、6502側は`DUTY_LOOKUP`が毎フレームデューティを進め既存の`WRITE_VOL_ONLY`（デューティは音量レジスタに同居）で反映する、`@t`/`w`はコンパイル時に音長へ反映済みのため専用オペコード不要、`SD`系はゲートオフとノート差し替えを1つにまとめた複合オペコード`0xE8`で対応（両者は仕様上必ず同フレームになるため、別々のオペコードとして並べると音長0のコマンドが生まれ6502側のカウンタがアンダーフローする）、`@vr`は既存の音量エンベロープ書込み機構(`WRITE_VOL_ONLY`)を再利用するため拡張音源(VRC6/MMC5/SUNSOFT 5B/FDS/N163)でも同時に対応、いずれも実機さながらの6502+APUエミュレータでブラウザ再生との完全一致を実測確認済み）。DPCMもNSF書き出しに対応済み（専用バンク配置+DMCトリガールーチン）。`!`/`!!`/`!!!`（データスキップ/シークバー用タイムシフト）と`x`（バイト直接埋め込み、このツールのバイトコードは実機ppmckと非互換のため対応する意味が無い）はNSF書き出しでは意図的に未対応。ループ最適化（繰り返し区間の圧縮、現状は展開済みセグメント列をそのまま書き出す）は未対応。詳細は`作業計画`フェーズ1.6参照
  - チャンネル割り当て: A=パルス1, B=パルス2, C=三角波, D=ノイズ
  - ノイズ(D)の音程は周期index 0-15（ppmck準拠、2026-09-18）: `n<num>`は直値、音符`c〜b`は半音番号がそのままindex（オクターブ・`K`は無視）。`D`/`EP`/`MP`/`PT`はindexへ8bitで加減算しクランプしない（`D16 n0`→`$F0`の桁あふれで短周期＝wikiの技がそのまま鳴る）。`@0`=長周期/`@1`=短周期は本ツール独自の明示指定（ppmckはノイズchの`@`をエラーにするため衝突しない）。`EN`は16で巡回（ppmckは12巡回でn12〜n15が壊れる）
  - 出力はチャンネル別・フレーム単位のAPUレジスタ書き込みログ
- `src/mml/player.js` — `MML.Mml.render(source, opt)`：コンパイル結果を6502/NSFを介さず直接APU2A03に適用し、音声波形を生成（`captureSong()`と互換の戻り値）
- `index.html` / `src/main.js` — 「MML作曲」パネルを追加（コンパイル結果表示、キャプチャして再生・シークパネルで試聴）

### Phase 5: 拡張音源対応（VRC6, VRC7, FDS, MMC5, N163, SUNSOFT 5B）

- `src/emulator/expansion/vrc6.js` — `Emu.VRC6Audio`：パルスx2（16段デューティ）+ サウトゥース波（アキュムレータ方式、/14分周）
- `src/emulator/expansion/mmc5.js` — `Emu.MMC5Audio`：パルスx2（2A03パルスと同形式、スイープなし、$5000-5007/$5015）+ハードウェアエンベロープ+$5011 8bit生PCM
- `src/emulator/expansion/fme7.js` — `Emu.FME7Audio`（Sunsoft 5B）：矩形波x3（AY-3-8910形式、$C000アドレスラッチ/$E000データ書き込み）+ノイズ(17bit LFSR)+ハードウェアエンベロープ+5bit対数DAC
- `src/emulator/expansion/fds.js` — `Emu.FDSAudio`：64サンプル6bit波形メモリ音源（$4040-407F波形RAM + $4080/$4082/$4083/$4089制御）
- `src/emulator/expansion/n163.js` — `Emu.N163Audio`：128バイト内部RAMによる波形音源（実機同様のインターリーブレジスタ配置、$F800アドレス/$4800データ）
- `src/emulator/expansion/vrc7.js` — `Emu.VRC7Audio`：emu2413(YM2413/OPLL)を忠実に移植した2オペレータFM音源（16音色テーブル、$9010アドレス/$9030データ）
- `src/emulator/nsfBus.js` — `opt.extraChips`（`NSF.CHIP_FLAGS`）に応じて拡張音源インスタンスを生成し、各チップのアドレス範囲へのレジスタ書き込みをルーティング
- `src/emulator/nsfPlayer.js` — `renderFrame()`で拡張音源も`clock()`/`mixSample()`し、2A03出力に加算
- `index.html` / `src/main.js` — NSFヘッダ入力パネルに「拡張音源」セレクトボックスを追加（none/VRC6/VRC7/FDS/MMC5/N163/SUNSOFT 5B）、選択値が`extraChips`ヘッダフラグに反映される

### Phase 5: MMLコンパイラの拡張音源チャンネル対応

- `src/mml/compiler.js`
  - `CHANNEL_BASE`を絶対アドレス化（A=$4000, B=$4004, C=$4008, D=$400C, ステータス=$4015）
  - `opt.expansion`（none/vrc6/mmc5/fme7/fds/n163/vrc7）に応じてチャンネルE以降を割り当て:
    - VRC6: E=パルス1 F=パルス2 G=サウトゥース
    - MMC5: E=パルス1 F=パルス2
    - SUNSOFT 5B: E/F/G=矩形波A/B/C
    - FDS: E=波形メモリ音源
    - N163: E-L=波形音源(最大8ch、波形は4の倍数の可変長)
    - VRC7: E-J=FM音源(6ch)
  - 各チップ用の周波数→レジスタ値変換（サウトゥース分周, SUNSOFT 5B周期, FDS周期, N163周波数レジスタ, VRC7 fnum/block）を実装
  - 拡張音源使用時は初期化レジスタ書き込み（波形ロード、ミキサー設定等）をフレーム0に追加
- `src/mml/player.js` — `MML.Mml.render()`が`opt.expansion`に応じて対応する拡張音源モジュールを生成し、レジスタ書き込みをAPU/拡張音源へ振り分けてミックス
- `src/mml/sampleMml.js` — 拡張音源チャンネル用にEチャンネル（DPCM: 定義と.dmcの扱い、音符=サンプル番号、止め方、分割）のサンプルを追加
- `src/main.js` — 「MML作曲」パネルのコンパイル/キャプチャ時に、NSFヘッダの「拡張音源」選択値を`opt.expansion`として渡す

### Phase 6: 波形エディタ、DPCMコンバータ、シンタックスハイライトUI統合

- `src/mml/waveformEditor.js` — `MML.WaveformEditor`
  - canvasベースの波形エディタ（FDS: 64サンプル×6bit, N163: 当初16サンプル×4bit固定）
  - ドラッグでの波形編集、サイン波/三角波/サウトゥース/矩形波/ランダムのプリセット適用
  - `getFdsWave()` / `getN163Wave()` で現在の波形データを取得し、`MML.Mml.compile()`/`render()` の `opt.fdsWave` / `opt.n163Wave` に渡して拡張音源の初期波形として使用
  - N163側はその後、可変長の共有バッファモデル(`src/mml/n163Alloc.js`)に対応した専用エディタ `src/ui/n163WaveEditor.js` に置き換わっている（FDS用は`waveformEditor.js`のまま）
- `src/ui/envelopeEditor.js` — `MML.UI.EnvelopeEditor`
  - 音量(`@v`/`@vr`)・音色(`@<n>={...}`を`@@<n>`/`@@r<n>`で選択)・ピッチ(`@EP`)・ノート(`@EN`)の4つを、
    共通の時間軸(横1マス=1フレーム)の4レーンとして1つのウィンドウで編集する。組み合わせて使う技なので、
    レーンごとのON/OFFで一時MMLを組んでまとめて試聴できる
  - 目盛り・音色の段数・試聴チャンネル・`#EX-`宣言は「対象音源」セレクタだけで決まる(テーブル定義は
    チャンネルに紐づかないため本文からは決められない)。存在しない組み合わせ(三角波の音量、パルス系以外の
    デューティ、VRC7のEP)はレーンごと理由付きで無効化する
  - 音量と音色のレーンは赤い縦線(キーオフ)から右がリリースの表。ノートレーンは「実際に鳴る音程の階段」を
    編集し、反映時にMMLの相対値へ変換する。和音生成ボタンはppmck公式リファレンスと同じ形
    (`@EN0 = { 0 | 4 3 -7 }`)のアルペジオを作る
- `src/mml/defBlocks.js` — `MML.Defs`
  - MML本文中の定義ブロック `@TAG<n> = { ... }` の走査・読み書きを1箇所に集めた共通モジュール。FDS波形エディタ / N163波形エディタ / VRC7音色エディタ / DPCMコンバータ（および今後のエンベロープエディタ）が共有する
  - `scan()`/`find()`/`indices()`/`enclosing()` で位置を探し、`tokens()`/`values()` で中身を読み、`format()` で整形し、`write()`/`erase()` でtextareaへ書き戻す（スクロール位置の保持とシンタックスハイライト更新用の`input`イベント発行も含む）。新規定義の挿入位置を返す `insertionOffset()`、試聴用の一時MMLへ持ち込む定義行を抜き出す `definitionLines()` も持つ
  - 値の意味（波形なのか音色なのかエンベロープなのか）は一切解釈しない。解釈は各エディタ側の責務
  - 行コメント(`;`以降)の扱いは`src/mml/lexer.js`と同じ：コメント内の `@FM0 = {` は定義として拾わず、コメント内の `}` は閉じ括弧として数えない（コメントアウトした定義をエディタが書き換えてしまうのを防ぐ）
- `src/dpcm/dpcmConverter.js` — `MML.Dpcm`
  - `DMC_RATE_TABLE_NTSC`（$4010レート0-15に対応する再生周波数テーブル）
  - `encode(samples, srcRate, rateIndex)`：PCM波形をリサンプリングし、2A03 DMCチャンネル用の1bitデルタ変調(DPCM)バイト列に変換（16バイト境界にパディング）
  - `decode(bytes, sampleCount)`：DPCMバイト列をプレビュー再生用PCM波形に復号
  - `hexDump(bytes)`：16進ダンプ生成
- `src/mml/syntaxHighlight.js` — `MML.Mml.highlight(source)` / `MML.Mml.attachHighlighter(textarea, overlay)`
  - チャンネル指定・音符・コマンド(`o l v q t @`)・オクターブ操作(`> <`)・タイ(`&`)・ループ(`[ ]`)・数値・コメントを色分け
  - textareaの背後にオーバーレイ`<pre>`を重ね、入力/スクロールに同期してハイライトHTMLを更新するオーバーレイ方式
- `index.html` / `src/main.js`
  - 「MML作曲」パネルのテキストエリアにシンタックスハイライトを統合
  - 行番号ガター(タイトル行の「行番号」チェック、`src/ui/editorLineInfo.js`)。OFFのときはエディタ右下に「行 N, 列 M」を表示。エラーログの `[Line N]` はクリックでその行へ移動
  - 「波形エディタ」パネルを追加（FDS/N163波形編集、プリセットボタン）
  - 「DPCMコンバータ」パネルを追加。現在は`src/ui/dpcmEditor.js`の`@DPCM<n>`定義エディタ（1定義=1行の表、ローカル編集→「反映」でMMLへ。詳細は使い方の節参照）
- `src/ui/keyboard.js` — `MML.UI.KeyboardDisplay`（鍵盤表示ウィンドウ: チャンネル一覧+大波形+ピアノロール+鍵盤）
  - レイアウトはタイトル行のボタン(⊞)から3軸で切り替えられ、localStorage(`mml_keyboardLayout_v1`)に永続化される
    - ピアノロールの向き: 縦(Synthesia式・音符が上から鍵盤へ降る、既定) / 横(DAW式・鍵盤を左に立て、音符が右から流れる)
    - ピアノロールの置き場: チャンネル一覧の下(既定) / 一覧の右 / 別ウィンドウ(`#win-pianoroll`。ツールバーのトグルはこの設定のときだけ表示)
    - チャンネル一覧: 1列(既定) / 幅に応じて自動で多段(チップ単位で列をまたがない)
    - ピアノロールの鍵盤: 全チャンネルを1つの鍵盤に(既定) / チャンネルごとに分割(使用ch数ぶんのレーン[ラベル+ロール+鍵盤]。各レーンは**そのchが曲全体で鳴らす音域**(+使っているドラムレーン)だけを音程軸いっぱいに表示するので、拡大率はレーンごとに違う(`_updateLaneRanges`)。音程方向のスクロール/自動追従は無い。レーンの大きさ(縦向き=幅、横向き=高さ)は既定で音域の広さに比例し(白鍵1本=15px、余りは比例配分)、レーンの境目のスプリッターをドラッグするとそのレーンだけ拡大縮小できる(ダブルクリックで既定に戻す。`_makeLaneSplitter`/`_applyLaneSizes`)。縦向きは横並び・横向きは縦積みで、収まらない分は`.kbd-lanes`全体がスクロールする。`_rebuildLanes`/`_drawRollCanvas(canvas,pos,onlyId,lane)`)
  - ロールと鍵盤は1つの「ロールペイン」(`_buildRollPane`)として置き場を移動し、描画は音程軸/時間軸→canvas座標の写像(`makeRollGeom`)で向きを吸収する。時間軸スケールは80px/秒固定なので、ロールが長いほど遠い未来まで見える
  - 一覧とロール/一覧と大波形の間はスプリッター(ドラッグ)で幅・高さを変えられる(`mml_keyboardListWidth` / `mml_pianoRollHeight`)。大波形は「下配置+1列」では一覧の右、それ以外では一覧の下の折りたたみ帯に置く
  - ロール本体をドラッグするとシークできる(縦向きは上下、横向きは左右。1px=1/80秒でつかんだ音符が指に付いてくる。`onRollSeek`→main.js `seekToSeconds()`)。ロール見出し行にはMMLエディタと同じシークバー(バッファ済み表示+青/赤の再生範囲ハンドル+時間表示)を副インスタンスとして置き、main.jsの`seekBars`で主(トランスポート行)と同期する
  - SPCボイス一覧のマスター値(エコー音量L/R、FIR C0-C7)はALL行の1セルにまとめてあり、他フォーマットと同じレイアウトに乗る
- **変換設定(全 *2MML 共通、`src/convert/options.js` + `src/ui/convertSettings.js`)**: 各「to MML」隣の「⚙ 変換設定」で、変換時に出すコマンドを選ぶ(localStorage `mml.convertCmd.v1` に保存、6形式共通)
  - 狙い: 熟練者が「ほぼ音階だけのプレーンな譜面」から自分で編曲を始められるようにする。プリセット「忠実再現」(既定、従来通り)と「プレーン譜面」(音階+音色のみ+譜面整形)、および個別チェック
  - チェック項目: ピッチ `D`/`EP`/`MP`/`PT`/`EN`、音量 `@v/@vr`(OFF時はピーク音量を`v`に)/`v`、音色 `@ OP MH N`/`s`、譜面整形(近似)「短い休符を吸収(1/32未満のゲート隙間、伸ばした区間は最後の音量のまま)」。旧「16分音符格子へ丸める」は2026-09-07に廃止(キーオン自体が格子から外れる曲にしか効かず、丸めれば必ずタイミングが崩れる)
  - **音符の区切り `NOTE_END`(2026-09-07)**: 「次の音符まで(既定)」/「音量ゼロで区切る(従来)」。抽出器は音量が0になった瞬間(またはキーオフ)に音符を閉じるため音長が `@v156 d+4&d+64.&d+192 r8.&r64` のような細切れになる。「次の音符まで」は音符の直後の休符を音符に吸収してキーオン間隔まで伸ばし、無音区間を2通りのどちらかで表す(`MML.Convert.applyNoteEnd`、`src/convert/envelope.js`。各*2mmlが`emitScore`の直前に呼ぶ)。どちらも再生結果が変わらない厳密な変形で、近似の譜面整形とは別物。(1) 減衰が自然に0へ到達した`@v`付き音符は表の末尾に0を1つ足す(同じ情報が表にもある二重表現を片方へ寄せる。コンパイラ`stepEnvelope`もNSFドライバも末尾値を保持し、0の位置はキーオンからのフレーム数なのでテンポ丸めドリフトの影響も受けない。「自然に」=最後の値が直前の減衰幅以下かつ最後の値の持続が直前の段+1以下。0を足した表は完全一致だけで登録し前方一致の共有/置換から外す`registerExact`、参照されなくなった表は`compact`で番号を詰める)。(2) それ以外(減衰途中で切られた`@v`、フラットな`v`、三角波、ループ表、VRC7のキーオフ)はゲートタイム`q<n>`/`@q<n>`(コンパイラはゲートオフを休符と同じに書き、NSF書き出しも`rest`ブレークポイントに分解する)。ゲートは状態コマンドなので切り替えを最小にする: 今の状態で表せるならそのまま、切り替えるなら先読みで最も長く持つ候補、1音しか持たない切り替えはしない(`q4 c4 q8`より`c8 r8`)、2拍を超える無音は休符のまま。スラー連鎖は連鎖単位で判定し先頭イベントに付ける。対象外: `@vr`付き(SPCは呼ばない。SPCは`q`を出していないため`@vr0`が鳴る経路が無い=別件)、曲末尾。伸ばした音符は`audibleEnd`に元の終端を残し、音程検証は可聴区間だけをサンプルする。実測(既定15秒・音長トークン数の減少): NSF -8.1%(166曲)、GBS -6.1%、HES -4.9%、KSS -2.9%、VGM -1.9%。悪魔城ドラキュラ1曲目60秒では 2651→2295、Batman 1662→1479。`@v`表は減衰途中で切られた音符の0付き表(exact)が前方一致共有と別枠になるぶん数%増える(NSF 1489→1570)。`NOTE_END=zero` の出力は変更前と全6形式・3551曲でバイト一致を確認済み。残る細切れは「減衰後サステイン→ゲートオフ(q/@qで表せない固定オン長)」「スラー連鎖(音程変化の間隔)」「テンポ誤検出」由来で本件の範囲外
  - **楽器化(2A03パルス、2026-09-08)**: 「複数の音符に割れていた1音を楽器定義へ畳む」段階。抽出器(`src/nsf2mml/converter.js extractPulseEvents`)の変更3点。(1) **リリース**: 固定音量モードで同じ音程のまま`$4003`を書き直し、その音量が直前以下で数フレーム内に確実に下がる(打ち直した音量より上がる瞬間があれば13→15→減衰の再アタックなので除外)書き込みは、mckドライバの`putReleaseEffect`(リリース区間の先頭をノートオンとして打ち直す)なので再アタックとみなさず`keyOffAt`だけ付ける。音符はそこで終わり、以後の音量列は`@vr<n>`(リリース表)、無音区間は`k<len>`(リリースが鳴る休符)で出す(NOTE_END=nextならゲートに吸収)。リリースが0に達しないまま無音になる音符は`releaseEnd`まで`k`、以降`r`(表に保持フレーム数ぶんの値を量産しない)。(2) **デューティ列**: デューティの変化では音符を区切らず`dutySeq`に積む。本体の変化が先頭4フレーム以内なら立ち上がりの音色として`@<n>={3 1}`+`@@<n>`、それ以外はループ検出込みの表。ただし「`$4003`無しでデューティを変えつつ音量を上げる」書き込みは位相リセットを避けた再アタック(Konami: 頭だけデューティ2・音量6)なので従来どおり新しい音符に区切る。(3) **リリース音色**: 固定音量の音符でデューティが途中で1回だけ変わりそのまま終わる(DQ2: 余韻で1→0)場合はそこをキーオフとみなし`@@r<n>`(実機`_REL_ORG_TONE`)+`@vr`で出す(以前は再アタックする別音符だった)。`@vr255`/`@@r255`への解除はゲートが音長いっぱい(q8かつ@k0)の間は出さない(リリースはゲートオフかkでしか発動しないので音は同じ)。Wing Defenders(mck製)は`@0={3 1}` `@v0={2 2 3|5 5}` `@vr0={4 4 3 2 0}` `@@0 @vr0 @v0 f+16k32 g16&g k64.&k …`に復元でき、NSF書き出し経由で元曲のレジスタ列と一致(違いはppmckの`$4003`打ち直しの位相リセットのみ)。検証は「元NSFのレジスタ状態(デューティ+音量)をフレーム毎に旧出力/新出力と比較」(scratchpadの`regstate-check.js`方式)で、悪魔城伝説P2 43→20、Crisis Force P2 15→12など旧出力より元曲に近づいた
  - **@v表の前方一致共有を末尾保持と整合させた(全形式、2026-09-08)**: `EnvelopeRegistry.registerShape`。`analyzeVolumeShape`は末尾の同一値を1点に切り詰めるため「5 4 3 を7フレーム保持」の表`{5 4 3}`が`{5 4 3 2 2 1}`の前方一致になり、共有すると保持していた区間で2 2 1が鳴っていた(悪魔城伝説で実測: 元曲は3を保持、旧出力は2まで落ちる)。切り詰めたフレーム数を`holdLen`として持ち、短い方が保持していた区間の値が長い方でも同じときだけ共有・置換する
  - **楽器化の共有化とMMC5/ノイズへの展開(2026-09-08)**: 判定と切り分けを`MML.NSF2MML.Instrument`(`src/nsf2mml/converter.js` 上部: `decaysAfter`/`isReleaseRewrite`/`isRetrigger`/`decideKeyOff`/`markSilence`/`endOf`/`volumeFields`/`toneFields`)へまとめ、2A03パルス・2A03ノイズ(`$400F`の打ち直しで同じ周期のまま減衰=リリース)・MMC5パルス(`src/nsf2mml/expansion/mmc5.js`、converter.js が `inst={vrReg,dutyReg,cmd}` を拡張音源抽出器の末尾引数で渡す)で共有する。検証は「元NSFの各フレームのデューティ+音量」を旧出力/新出力と比較(scratchpad `regstate-check.js`、±1フレーム)
  - **印無しのリリース切り出し(全形式、2026-09-08)**: キーオン打ち直しの印が無いチップ(PSG/SCC/GB/PCエンジン/SN76489)向けに、音量列の「サステイン(2フレーム以上同値)の後、終わりまで下がり続ける尾」をリリースとみなして `@vr` へ切り出す(`MML.Convert.detectRelease`、`EnvelopeRegistry.volumeFieldsWithRelease`。条件: 落差2以上、または(サステイン3フレーム以上・尾2フレーム以上で)尾の最後がサステインの1/3以下。尾が3フレーム未満は切り出さない=音符+`k`の2トークンに分けると音価の丸めで境界が±1フレーム動くため、短い尾は従来どおり表の末尾に置く)。各抽出器の `toVolumeFields` は `volumeFieldsWithRelease` を呼ぶだけで、終端の反映(`keyOffAt`)は `applyNoteEnd` 冒頭の `applyReleaseSplits` が全形式共通で行う(次の音符がスラーなら分割を取り消して従来の1本の `@v` に戻す/分割した音符がスラー連鎖の途中なら `@vr` を連鎖の先頭へ付ける)。`@vr` 表は `@v` レジストリが持ち `defLines` が一緒に出すので各形式のヘッダ生成は変更なし。借用(`borrow.js`)の写像プロキシも同じ入口を持つ。2A03は打ち直しの印を優先し、無ければこれ。副産物で分かった旧出力の欠陥: スラー連鎖は先頭の音量しか使わないため、連鎖末尾の音符の減衰(8→4→2)が旧出力では鳴っていなかった(AMHE.gbs)。新出力はリリース切り出しでそこが改善する
  - **KSS/VGM の AY・SCC に書込み時刻付きリサンプル(2026-09-08)**: HES/VGM-PCE の位相エイリアシング対策(`hes2mml/expansion/wave.js resampleSeq`、`MML.Convert.TickResample` として公開)を AY/SCC 抽出器(`kss2mml/expansion/ay.js`, `scc.js`)へ展開。書込み時刻は writeLog の形を変えず `Emu.kssPackWrite` の bit25-30 に 1/64 フレーム刻みで詰める(KSS: `KssPlayer.frameSampleFrac`、VGM: `vgmPlayer.js` の `kpk`)。旧ログは frac=0 で従来どおり。効果は小さい(Aleste PSG 6→4、Clash Pennant Race 2 SCC 42→41、Cabin Panic 61→58)。**残る変種の真因**: 駆動は VSYNC 同期(PLAY 周期 ≈1 フレーム)だが、エンベロープが 2.33 フレーム周期等の**自走タイマー(音符開始でリセットされない)**で進むため、段の位置が音符の開始位相ごとに違う(3,2,2 / 2,3,2 / 2,2,3 の3変種)。これは本当に別のフレーム列なので、ノート相対のリサンプルでは揃わず、ppmck の `@v`(フレーム毎の絶対値)では表現しきれない。揃えるには「段の並びが同じで段の長さが±1違う表を1本にまとめる」近似統合(音が最大1フレーム変わる。既定OFFの別オプション)が必要
  - **スラー連鎖の音量列連結(全形式、2026-09-08)**: タイ(&)で繋いだ連鎖はコンパイラで1セグメントになり音量は先頭の `@v`/`v` しか使われないため、後続の音符の減衰や音符境界の音量ディップ(GB AMHE: 各音符の末尾1フレームが8→4)が旧出力では消えていた。各音符が残す生の音量列(`_volSeq`、リリース分割後は本体ぶん)を音符長で繋いで1本の表として登録し直し先頭に付ける(`MML.Convert.mergeSlurVolumes`、`applyNoteEnd` 冒頭で `applyReleaseSplits` の直後。ハードウェア減衰表やSPCなど生の列が無い音符を含む連鎖は触らない)。後続の音符の表参照は捨て、`envReg.compact` を NOTE_END に関わらず最後に掛けるので未参照の表も消える。効果は大きい: 元NSFのデューティ+音量のフレーム照合で Contra P1/P2 の不一致 169/116→0、悪魔城ドラキュラ 48/1→0(Konami のレガート主体の曲)
  - **似た`@v`表の近似統合 `ENV_MERGE`(譜面整形(近似)、既定OFF、2026-09-08)**: 段の値の並びとループ位置が同じで、各段の長さが±1・全体長も±1以内の表を、最も多くの音符が参照する変種へ寄せる(`EnvelopeRegistry.mergeSimilar`、`@vr` 表にも適用。ハードウェア減衰表と exact 表は対象外)。ドライバの自走タイマーで段の位置が音符の開始位相ごとにずれる曲(MSX の Cabin Panic: 3,2,2 / 2,3,2 / 2,2,3)向けで、段の境目が最大1フレーム動く近似。効果: Cabin Panic 58→23、NCS63001.hes 147→132、Clash Pennant Race 2 42→38
  - **SPC の ADSR は KON 中の実測列(`@v`)で完結、`@vr0` は廃止(2026-09-08)**: 実機 DSP のキーオフ後リリースは毎サンプル env-=8 で最大値からでも約256サンプル=8ms(1フレーム未満)で 0 に落ちるため、フレーム単位の表では {0} になり、q8 の音符では鳴る経路も無かった(`simulateSpcRelease` は最大値起点の近似で実際のレベルとも無関係)。SPC の音符を「`@v` 付き・キーオフで終わる」だけの他形式と同じ形にし、`applyNoteEnd`(音符の区切り: 末尾0の吸収と `q`/`@q`/`@k` ゲート)を SPC にも掛ける。`@v` 定義は `emitScore` の headerLines へ移した(吸収が表を書き換えた後に出す)
  - **ゲートを揃える `GATE_APPROX`/`GATE_TOL`(譜面の書き方(近似)、既定ON・許容2フレーム、2026-09-08)**: `NOTE_END=next` のゲート候補に、キーオフ位置のずれが `GATE_TOL` フレーム以内の `q<n>` も許し(`gateCandidates(D, A, tol)`)、動的計画法のコストを「休符/`k` トークン=3、状態コマンド=4、ずれ1フレーム=0.5、`@q`/`@k` は `q<n>` より+0.5」にして、チャンネルの大半を1つの `q`(または `@q<n>`)で書く。厳密一致だけだとドライバのキーオフが比率でも固定フレームでもない曲で `k` が細切れに残っていた(Famicompo「Wing Defenders」: 13/76/51フレームの音符が全て「次の4フレーム前」→ `@q4` 一本、202フレームだけ `@k192`)。レガートと長い無音は切らない。OFF なら以前の厳密一致のみ
  - **無音で終わるリリース表に 0 を足す(2026-09-08)**: リリースの最終段が短い(末尾値の連続が3フレーム以下)まま無音になった音符は、その無音がリリース表の最終段 0 そのもの(`{4 4 3 2}` → `{4 4 3 2 0}`、`MML.Convert.releaseWithZero`)。以前は「表が 0 に達しないまま無音」(`releaseEnd`)扱いで `k…r…` 書きになり、ゲートで吸収できなかった。末尾値を長く保持してから切れる列は従来どおり `releaseEnd`(0 を足すと保持長ごとに表が量産されるため)
  - **音長を丸める `LEN_SNAP`(譜面の書き方(近似)、既定2フレーム、0=厳密、2026-09-08)**: `framesToLengths` のマッチ許容誤差(従来は192分の半分=5tick固定)をこのフレーム数まで広げ、大きな音価1個で書けるならタイの列(`f+4&f+2&f+8..&f+64.&f+192`)にしない。余りは `carryOut` で次の音符へ持ち越し(±許容に収め、超過は捨てる)、音価の一致は「持ち越し込みで許容の2倍以内、最も近いもの」で選ぶので、各境界のずれは最大このフレーム数、音符自身のずれが許容内なら必ず1個の音価に乗る。小節線から許容以内で始まる/終わる音符は小節線で割らない(`splitAtBoundaries`)。ドライバのテンポが小数(ppmck の t71 = 4分50.8フレーム)で音符長が ±1〜2 フレーム揺れる曲に効く
  - **テンポ推定の見直し(2026-09-08、`src/convert/bpm.js`)**: 材料を音長(end-start)から発音開始間隔(IOI)へ(`MML.Convert.tempoMaterial`、音長はチャンネル最後の音符だけ)。ゲートで切られた音長がグリッドを引っ張り、ゲート長のほうを16分音符とみなす速いテンポを選んでいた(Wing Defenders t106、真は t71)。さらにカバー率の一致1件を「その音価が素直に書けるか」の重み(`simplicity`: 2の冪=1.0、付点=0.85、タイ/複付点=0.6、他=0.4)で数える。細かいグリッド(tick 2〜3フレーム)は整数の音価をほぼ何でも説明できてしまい、カバー率だけでは常に勝って譜面が付点とタイだらけになる(t200 が選ばれていた)ため。倍/半テンポの判定は従来どおり典型テンポ帯の事前分布に任せる。回帰ベースラインのテンポは広く変わる(意図した変更)
  - **BPM推定を混合モデル尤度方式へ全面改訂(2026-09-09、`src/convert/bpm.js`)**: 旧方式は「1/32音符グリッドに IOI の何%が乗るか」のカバー率で、(1) 三連(4分の1/3)が原理的にグリッドに乗らず三連の多い曲を当てられない、(2) 許容誤差がグリッド比例で ±1フレームのジッタがある曲(SPC等)が半分のテンポに転ぶ、(3) 説明できない IOI が細かいグリッド=2倍テンポを有利にする、という構造的な欠陥があった(合成36曲の往復テストで正解 10/36)。新方式は各 IOI を「音価 k×(4分/24)+ジッタ σ0.6フレーム、または外れ値 ε0.1」の混合分布で説明する対数尤度を整数BPM 40〜400 で全探索し(音価の事前重みは 4分=8分=16分=1.0 でオクターブ中立、三連は低め、出現数は√で圧縮、テンポ事前分布 0.8·log2(bpm/125)²)、最良候補の近傍で4分音符のフレーム数(小数)を精密化する。2倍/半分は各 *2mml が `MML.Convert.chooseTempoOctave`(`mmlEmit.js`)で「B/2・B・2B で実際に音価を書いてみて、譜面の複雑さ+事前分布が最小」の候補を採る(小節分割はしない。割るとタイが遅いテンポほど減って不公平)。合成36曲で BPM正解 30/36・格子一致 36/36・音長一致 97%(正解テンポの曲)、実曲39曲で譜面が簡素化 17勝5敗。ベンチは `tools/headless/tempo-bench.js`。処理時間 +40ms 程度
  - **音長をチャンネル全体で最適化 `LEN_DP`(譜面の書き方、全コマンド=ON / プレーン譜面=OFF、2026-09-09)**: `framesToLengths` を音符ごとに呼ぶ greedy は直前の余りだけを見るので、速いテンポで 5,5,5,6 フレームと揺れる16分が `24..` に化け、3連8分の隣で持ち越しが逆向きに溜まると `16.` になる。`MML.Convert.quantizeSeq`(`duration.js`)はチャンネルの全イベント列を見渡し、「音価の書きにくさ(付点 +0.5、3連系 +0.15、64分以下 +1、タイ +0.2)+境界の位置ずれ(フレーム)²×0.5」の合計が最小の割り当てを動的計画法で選ぶ(状態=書いた累積tick、遷移=1〜3トークンの和、境界のずれは常に `LEN_SNAP` 以内)。合成曲で音長一致 91%→97%。greedy は持ち越しの超過分を捨てて黙ってずれるのに対し DP は厳守するので、格子に乗らない音符の多い実曲では 3連系やタイが少し増える(忠実さ優先、プレーン譜面では OFF)。あわせて音価テーブルに 3連2分(3)・3連4分(6)を付点なしで追加(無いと 3連4分が `12..`/`8.` の交互に化ける。付点つきを許すと 2進の音符が `3..&24` に食われる)
  - **変換設定ダイアログの整理(2026-09-08)**: 「ややこしくなった」との指摘で1段組に再構成。プリセット+変換テンポ → 出すコマンド(チェック付きチップ、説明はホバー) → 譜面の書き方(音符の区切り/ゲートを揃える+許容/音長を丸める+許容/短い休符を吸収/似た@v表を統合。「(近似)」付きは音が数フレーム変わりうる整形) → 詳細設定(折りたたみ: ピッチ精度 SA/N163波形/基準ピッチ+最小偏差)
  - **変換設定ダイアログの再整理(2026-09-18)**: 1画面に収める。プリセット|変換テンポ(横並び) → 変換ログ → 出力コマンド(チップ。「打楽器パート」はMMLコマンドではなくドラムパッド(DPCM(E)/ノイズ(D))の打点をパート化する経路のON/OFF) → 譜面の書き方(折りたたみ) → 出力の書式(折りたたみ) → 詳細設定/N163(折りたたみ)。グループは薄い色で塗り分け、折りたたみの開閉は localStorage `mml.convertSettings.open.v1` に覚える
  - **ループを自動検出(2026-09-19、変換設定「出力の書式」、既定OFF、`src/convert/mmlEmit.js detectLoop`)**: 元曲のループ周期を全チャンネルの音符列から検出し、イントロ+1周ぶんだけを書き出して各チャンネルのループ開始位置に `L` を置く。5分ぶん変換しても曲データが1周ぶんで済む(実測: Crisis Force 300秒 41,088→20,608バイト、グラディウスII 120秒 28,800→12,416バイト)。**イントロとループ1周のフレーム数は全チャンネルで一致させる**(NSFは各チャンネルが自分の `L` へ独立に戻るので、1フレームでも違うと周回のたびにずれていく): 音長の計画をイントロ/ループで別々に立て、境界直前の音長を192分音符単位で伸縮し、コンパイラと同じ丸め(`duration.js simulateCompiledFrames`)で数えたフレーム数を共通の目標に合わせる。合わせられなければループ化をやめて通常出力に戻す。`L` は音符をまたがない小節線を優先して選び、またぐ音符はタイにせず打ち直す(タイの途中の `L` はNSFでループ先が登録されない)。`L` の直後では音量/音色/オクターブ/EP等の状態コマンドを出し直す。キャプチャが「イントロ+2周」より短いと確認できないので検出しない
  - **オクターブの移動は必ず `>`/`<` で書く(2026-09-21、`src/convert/mmlEmit.js`)**: 従来は±1オクターブだけ `>`/`<` で、2オクターブ以上の跳躍は絶対値の `o<n>` にしていた。これだと**音域外の警告(下の「借用先の音域外は鳴らさない+警告」)を避けるためにチャンネルの頭でオクターブを上げても、途中の `o<n>` でその場の高さへ引き戻されてしまう**。跳躍ぶんだけ `>`/`<` を並べる形に変え、絶対値が出るのは**チャンネルの先頭と `L` の直後だけ**(`forceReemit` で `curOct=-1` にする箇所。ループで戻ったときの高さを確定させるために残す)になった。これで先頭の `o<n>` を1個書き換えればチャンネル全体が移調できる。コンパイル時にオクターブは絶対音程へ畳まれるので**バイトコード/NSFの大きさは変わらない**。実測(NSF/SPC/GBS/HES/KSS/VGM 21曲、変更前後でコンパイル結果のレジスタ書き込み列をハッシュ比較): 21曲すべて音は完全に同一、途中の `o<n>` は 378個減り、MMLの文字数は合計+6字(跳躍の多いHESの1曲だけ+134字、他は同じか減る)
  - **NSF書き出しの `L` まわりの修正(2026-09-19)**: (1) `@vr255` の解除オペコードを、`@vr` を一切使わないチャンネルの `L` 直後にも出してしまい、`L` 以降がでたらめな音になっていた(同日のコミットで混入、`mckBytecode.js chUsesVr`)。(2) ドライバのサイズ計測にループ情報を渡しておらず、`L` を使う曲でドライバがバンク境界をまたぐと「計測時と再アセンブル時でサイズが一致しません」になっていた(`ppmckDriver.js probeLoop`)
  - **1音が複数の音符に割れる不具合の修正(2026-09-19、全て「元曲の書き込み列 vs 変換MMLのコンパイル結果」のフレーム突き合わせで実測)**: (1) **VRC6**(`nsf2mml/expansion/vrc6.js`): 周期上位(`$9002/$A002/$B002`)の書き込みを無条件に再アタックと数えていた。VRC6 はこの書き込みで位相が戻らないので、同じ音符のまま音量が「同じか1だけ下がる」書き直しは区切らない(それより大きく下がる書き直しは、小さい音量から始まる音色の同音連打でありうるので区切る。Konami のドライバは長い音符の途中で同じ値を書き直す。`@v13 d8 v3 d8.` → `@v13 d4&d16`)。ビブラート中の毎フレームの書き直しも同じ理由で1音になり EP へ回る。デューティの変化は 2A03/MMC5 と同じく区切らず `@@<n>`(デューティエンベロープ)にする(音符の頭1フレームだけデューティ0の音色が「96分音符+本体」に割れていた)。悪魔城伝説 曲1: パルス1の音量の食い違い 553→230 フレーム、デューティ 226→78。(2) **再トリガー判定**(`converter.js Instrument.isRetrigger`、2A03/MMC5/VRC6 共通): 「デューティ変化+音量上昇」を再トリガーとみなす規則が、音符の頭(頭だけ別デューティ)の次のフレーム(本体のデューティへ戻りつつ音量が上がる)にも当たって1音を割っていた。始まって4フレーム未満の音符では判定しない。魍魎戦記MADARA: パルスBの音量の食い違い 182→35、VRC6パルス2 1048→112。(3) **SUNSOFT 5B**(`expansion/fme7.js`): 打ち直しの検出が無く(N163/KSSのAYには有る)、音程変化が全部タイになって2音目以降の `@v` が消えていた。音程境界の音量の跳ね上がりでタイ候補から外し、同じ音程のランは `splitRetriggers` で分ける(音符の中のエコー=頭より2以上小さい跳ね上がりは分けない)。ギミック! 曲1: 音量の食い違い 1151→182 フレーム。(4) **GBS パルス**(`gbs2mml/expansion/pulse.js`): GB はトリガーし直さないと音量が変わらないので、ドライバはソフトウェアの減衰を「音量を下げてトリガー」で作る。これを全部新しい音符にしていた(`a+ v10 a+ v8 a+ v6 a+`)。同じ音程・同じデューティで、初期音量(NRx2)を前回のトリガーより下げたトリガー(と、始まって4フレーム未満の音符へのトリガー)は区切らない。★鳴っている音量と比べるだけだと、小さい音量から膨らむエンベロープの同音連打(魔界塔士サガ)まで1音に統合してテンポ推定が狂う
  - **音符の頭だけ音程がずれる音符が丸ごと外れる不具合(2026-09-19、`convert/pitch.js classifyPitchMod`)**: `D<n>` は音符の先頭フレームの音程で決まる。頭の1〜3フレームだけずれてあとは一定の音符(ビブラートの最後の1フレームから始まる音符など)は、変化が短すぎて EP にならず「変調なし」になり、音符のほぼ全体と、タイで繋がる後続の音符(タイの2音目以降は自分の D を持てない)が頭のずれ分だけ外れて鳴っていた(HES NX91002 曲34 のQパート: +33〜40セントが約1秒続く → ±4セント以内)。保持する値までの短い非ループ EP にする。あわせて、末尾を長く保持する非ループ EP は前方一致で他の表と共有しない(共有は「音符が表より先に終わる」前提。保持する音符に続きのある表を当てると、止まるはずの音程が動く)
  - **タイの2音目以降が先頭音符の D で外れる件(2026-09-20、`convert/detune.js splitSlurOnDetune`、`applyNoteEnd` の冒頭で呼ぶ)**: タイ(&)で繋いだ2音目以降は自分の `D` を持てない(コンパイラは異音程のタイを1音符内の音程切替として鳴らし、`D` は先頭のものを使い続ける)。2音目以降が先頭と違うずれ方をしていると外れる(実測: GBS で最大18段、HES で31曲700音符)。先頭の `D` で鳴らしたときのずれが10セント以上で、自分の `D` のほうが3セント以上ましな音符だけタイを切って打ち直す(聞き分けられない差のためにレガートは失わない)。1段が何セントかは `D` を決める処理が音符ごとに控える。元曲との音程の突き合わせ(10セント以上ずれたフレーム数、GBSの波形→FDS): Last Bible II 134→3、Last Bible 92→16、CGB-AZ7J 147→3、カービィのピンボール 136→117。★ppmckの `&` は「次の要素の音長を足す」だけで、異音程のレガートは本ツール独自(ppmckでは2音目の音程が捨てられる。ヘルプに明記)。ppmckの周波数表(1オクターブ表の右シフト)との±1の違いもppmckには合わせない(ユーザー決定)。★副作用対策: タイを切ると、それまで音量をまとめられなかった連鎖がまとめられるようになり、音符ごとに同じ減衰をかけ直すレガート(火の鳥 鳳凰編の分散和音)が長い音符で周期の位相を崩して360値のループ無し表になった(1597→2944字)。`mergeSlurVolumes` は、まとめた列がループにならず約3秒(`MAX_ENV_FRAMES`)より長いときは、崩れる前の先頭部分(180〜36フレームの最長)がループになり、そのループ表を連鎖全体に当てたときの食い違いが15%以下ならそれを使う。それ以外は従来どおり厳密な長い表でまとめる(音符ごとに音量のアクセントが違う連鎖を1周期に丸めない。TP04022.hes は食い違い38%で厳密なまま)。火の鳥は `{| 7 7 7 7 7 7 7 7 6 5 4 3}` で1684字、元曲との音量・音程の食い違いは変更前と同数
  - **曲が終わったあとの無音を休符で埋めない(2026-09-19、`convert/mmlEmit.js emitScore`)**: 全チャンネルで最後に音が鳴り終わる位置を曲の終わりにし、各チャンネルの末尾の休符を落とす(以前はキャプチャした秒数ぶん休符が続き、再生もNSFも無音を最後まで演奏していた)。DPCM の最後の一打は「次の一打まで」の長さで来るので1秒で切る。リリース(`@vr`)つきの音符の余韻を鳴らしている後ろの `k` は落とさない(`releaseEnd`、無ければ2秒まで)。ループ自動検出でループ化するときは全チャンネルの全長を揃える必要があるので対象外
  - **VRC7 自作音色の同時使用解決は「次の音符まで」を見込む(2026-09-08)**: `Vrc7Tone.resolveConflicts` は各 *2mml で `applyNoteEnd` より前に走るため、抽出時点の音符終端で重なりを見ると、ゲートで伸びた先で別の自作音色と重なりコンパイル不能になった(Lagrange Point 14.9秒 I/L)。区間の終わりを同chの次の音符の始まりまで(上限120フレーム=maxGap相当)伸ばして判定する(`customIntervals`)
  - **出力の書式 `PART_ORDER`/`BARS_PER_LINE`/`BAR_ALIGN`(2026-09-08、プリセット外)**: パートの並び(「チャンネル順に小節ブロック」=全パートを数小節ずつ縦に並べる(既定)/「パートごとにまとめる」=Aを最後まで書いてからB)、1行の小節数(1〜16、既定4)、小節を揃える(全パートで桁揃え。既定OFF=スペース1つで区切る。★2026-09-08にユーザー指定でOFFが既定)。`mmlEmit.js emitScore` が `opts.cmd` から読む。小節頭の区切り空白は落とす
  - **チャンネルの並び順 `CHANNEL_ORDER`(2026-09-09、プリセット外)**: 「アルファベット順」(既定、パート文字A,B,C…順)か「変換元の割り当て順」(元の音源のチャンネル順)。各 *2mml が `scoreChannels` へ積んだ順を `MML.Convert.stampChannelSource` が `srcIndex` として刻み(従来の `sort((a,b)=>a.letter…)` は `MML.Convert.sortChannelsByLetter` に集約)、`emitScore` が `orderChannels` で出力直前に並べ替える(呼び元の配列は変えない)。例: MDのVGMは letter=`DGHIJKLXYZ` / source=`GHIJKLXYZD`(Dはドラム=最後に足したパート)。★文字の比較はコードポイント順(大文字A-Zのあとに小文字a,b)。`localeCompare` は `a` < `B` と判定するため出力が `aAbBCDEFG` と大小交互になり音源ごとのまとまりが崩れる(2026-09-10修正。同じ理由の前例が `channelPlan.js sortByLetter`)。小文字a,bはMMC5の2ch(`compiler.js LETTER_POOL` = E:DPCM / F:FDS / G-L:VRC7 / M-O:VRC6 / P-W:N163 / X-Z:SUNSOFT 5B / a,b:MMC5)。★SPCは唯一レター順に並べ替えていなかったので、既定でも並びが変わる
  - **変換設定ダイアログの読みやすさ(2026-09-09)**: 「説明が横に長い/項目の区切りが不明瞭」という指摘を受け、各行の説明を短文にして元の長文は行の `title`(ホバー)へ、セクションは枠+見出しの下線、行間に区切り線、セレクトは名前の右側(3列目)へ移して全行の桁を揃えた
  - **DPCMドラムの音量正規化(`src/convert/drumHits.js`、2026-09-09)**: 「SPCだけDPCM変換時の音量が小さい」という指摘の実測(全形式の`MML.Dpcm.encode`へ渡る波形を計測)で、クリップのピーク中央値が HES 0.98 に対し SPC 0.13(RMS 0.39 対 0.028、約18dB差)と確認。DMCには音量指定が無く焼いた波形の振幅がそのまま音量になるのに、素材の振幅スケールが形式ごとに違うのが原因だった(HESのDDAは5bit値を[-1,1]へ写すので常にほぼ全振幅、SPCのBRRはピーク中央値0.43で`vol`もVOLレジスタ/127=中央値0.29)。曲内で最も大きい打点が全振幅に届くよう曲全体へ同じゲインを掛ける(打点ごとの強弱比は保つ、持ち上げのみ、上限12倍、1.05倍未満は無変換=既に全振幅の形式は出力不変)。上限はSPC40曲で必要ゲインを測って決めた(中央5.3/90%点9.7/最大12.2)。実測でSPCはピーク0.126→0.807・RMS 0.028→0.194(約+17dB)、HESは normGain=1.00 で出力不変
  - **長いDPCM(ストリーム再生)の分割(`src/convert/drumHits.js`、2026-09-09)**: DMC 1本の上限は`$4013`の4081バイト(33.1kHzで0.98秒)で、以前はこれを超えるクリップを`MAX_CLIP_SEC=1.5秒`で切ったうえコンパイル側が`$4013=255`で頭打ちにして途中から無音になっていた。共通層`dpcm()`が上限を超えるクリップを**フレーム整数の区間へ分割**し、区間ごとに`@DPCM`定義(`<名前>_<区間番号>.dmc`)と打点イベントを立てる。Eチャンネルは`n5 n6 n7 …`と並び（音符の番号=`@DPCM`番号）、実機DMCは次のトリガーまで鳴り続けるのでそれだけで連続再生になる(ドライバ側の変更なし)。区間の`$4011`初期値は直前の区間を焼き終えたDACカウンタ(`MML.Dpcm.endCounter`)にして継ぎ目のプチ音を防ぎ、ピーク正規化はクリップ全体で1回。区間の打点は`exact:true`で、出力側(`mmlEmit.js`/`duration.js`)が**音長の丸め(LEN_SNAP/LEN_DP)から外して厳密に書く**(変換設定`DPCM_EXACT`、既定ON。チェーン内の持ち越しを閉じ、チェーン直前のイベントは位置を厳密にするので、合成テストではテンポ128/150・LEN_DPのON/OFFいずれも継ぎ目のトリガーずれ0フレーム)。全ての`.dmc`は4080バイト以下になる。DMC領域は`$C000-$FFFF`の16KBだが、それを超える曲は**16KBごとの「ページ」にサンプルを分けて置き、トリガーの直前に窓4-7(`$5FFC-$5FFF`)をそのページへまとめて切り替える**(2026-09-10、`compiler.js layoutDpcmSamples`のページ番号+`ppmckDriver.js`の`DPCM_PAGE_TBL`/`WFV_T28`。区間は必ず別トリガーでDMCを止めてから始まるので、読出し中の切替は起きず継ぎ目の品質も落ちない。ブラウザ再生も同じ`$5FFC-$5FFF`疑似書込みで`buildDpcmBus`のページを切り替える)。HE93002(DDA 26秒=109KB)で7ページ31バンクのNSFが実機同型エミュレータで全バイト一致を確認。パネルの合計表示は16KB超を黄色(ROMが大きい)で知らせる。ドラム(DPCM)パネルの行を選ぶと下段に分割ビューが出て、境目・区間ごとの使用/レートを手で決められる(サンプル単位に秒で保存)。**32X PWM(VGM)のストリーム**は`0xB2`直書きで開始アドレスのような同定情報が無いので、キャプチャがサンプル列そのものを記録し(`vgmPlayer.js pwmRec`→`data.pwmStream`)、`main.js vgmPwmStreamDrumFor`が無音の切れ目(0.25秒)でクリップに分けて「クリップ1本=打点1個」にする(行`PWL`に集約、既定E)。After Burner Complete「Sega Logo」の66秒ストリームが1クリップ→自動11区間(先頭10秒)→Eパートに`@0..@10`のトリガーで出た
  - **ノイズパッド(2026-09-18)**: ドラム(DPCM)パネルの各パッドに「載せ先」列(DPCM/ノイズ)。ノイズにしたパッドの打点は、プリセット(周期n0-15・長短@0/@1・音量v/@v・@EP・@EN・Dの組み合わせ。組み込み7種+ユーザー追加、`src/convert/noisePresets.js`)の音色で2A03ノイズ(D)の音符列になり、元曲のDと単音マージされる(`src/convert/drumHits.js noise()/applyNoise`。後着が前を切る、同時は「優先」列の高い方、同点はパッド勝ち)。同じ音色の連打で@v等が並ばないのは出力側の状態差分に任せる。音色エディタ(✎)の試聴は本物のコンパイラ+2A03エミュ(`MML.Mml.render`)で鳴らす。鍵盤の割当で旋律chに **D(ノイズ)** を選んだ場合も E と同じく打楽器化されてパッドに並び(入口は1つ)、載せ先の既定は「割当どおり」(E→DPCM、D→ノイズ)、D割当パッドの音色の既定は「音程から自動」(従来の音程→周期の写しと同じ出力)で、プリセットに変えたパッドだけ差し替わる。一覧と下段の境目はドラッグで動かせる
  - **ブラウザ再生でDPCMのページ切替が効いていなかった(2026-09-22修正、`src/emulator/apu2a03.js`)**: `APU2A03.writeRegister` は `$5FF8-$5FFF` を `this.bus.write` へ回す作りだが、コンストラクタは `bus` を `DmcChannel` に渡すだけで **`this.bus` を自分に持っていなかった**ため、ページ切替の疑似書込みが黙って捨てられていた。`@DPCM` の合計が16KBを超える曲では、2ページ目以降の定義が「同じアドレスにあるページ0の音」で鳴る(R-Type Leo 2曲目で GA20 PCM1 と FM6 の打点を E へ重ねると、FMのミックス定義が2ページ目の `$C000` に載り、ページ0先頭の別のサンプル群を読んで鳴っていた)。NSF書き出しはドライバ(`DPCM_PAGE_TBL`/`WFV_T28`)が切り替えるので元から正しい。検証は「DMCがバスから読んだバイト列をトリガーごとに控えて、その音符が指す定義のバイト列と比べる」: 修正前は2ページ目のトリガーが0/2、修正後は2/2、ページ0は前後とも66/66。NSFは6502+APUエミュレータで走らせて61/61。`apu2a03.js` は NSF/VGM のキャプチャWorkerバンドルに入っているので再ビルド済み
  - **鳴り終わりかけの打点はミックスしない(2026-09-22、`drumHits.js dpcm()` の `TAIL_FRAMES=2`)**: 新しい打点の頭で、前から鳴っていてあと2フレーム以内に鳴り止む打点は、その打点に切られたことにして混ぜない(実機DMCの打ち直しと同じ)。混ぜると「しっぽ1フレーム+新しい打点の全長」が新しいミックス定義として丸ごと焼き直される(上の曲で、FMの打点が1.87秒のループ素材の頭より1フレーム長く残っただけで、ループ素材全体=2分割7.7KBがもう1組定義されていた。150秒の変換で12定義34.3KB→10定義26.6KB)
- **MMLエディタの追尾チャンネル表示(2026-09-09)**: 再生位置ハイライトは全チャンネル同じ白で「どれを追尾しているか分からない」ため、追尾チャンネル(`mmlFollowChannel`)だけ別色(`--follow-fg`/`--follow-outline`、既定は黄。色設定の「追尾チャンネルのハイライト文字/縁取り」で変更可)にした。行番号ONのときはガターの行番号も同じ色で追随する(`src/ui/editorLineInfo.js setPlayingLineEl`、追随スクロールのON/OFFとは独立)。あわせて停止時にハイライトが消えていなかった取りこぼしを修正(`transportStop`がrAFを止めるため後始末が回っていなかった)
  - **変換設定ダイアログは見出し行のドラッグで動かせる**(`convertSettings.js makeDraggable`、位置は次に開くときも保持)
- **ミニ操作窓とOSのメディアコントロール(`src/ui/miniTransport.js`、2026-09-12)**: ヘッダ右のミニ操作窓ボタンで、**常に手前に出る小さな再生操作窓**(Document Picture-in-Picture)を開く(同じボタンをもう一度押すと閉じる。本体ページ側には一切出さず、閉じているあいだ中身は非表示のまま本体の文書に置いてある)。中身は[ファイルを開く]⏮▶⏭■・MML/ファイル切替バッジ・曲が終わった後の挙動・曲名・×・シークバー・chミュート。chミュートは**既定で畳んであり**、見出しは`▸ chミュート (29ch)`、消していれば`— 1個ミュート中`が赤字で付く。各chのボタンは**ch一覧と同じ色**(鍵盤表示の行の丸と同じ値。ユーザーが変えれば追随)を薄く敷いて左端に色帯を出す。一覧は曲が使っているチャンネルの集合で、瞬間の発音では増減しない。操作は全部**鍵盤表示タイトル行と同じ入口**(`keyboardDisplay.onTransport`ほか)へ流すので実装は二重化していない。表示の更新は小窓側の250msタイマーで回す(本体タブが最小化されると本体のrAFが止まるため)。**`requestWindow()`はユーザー操作必須**(無操作だと`NotAllowedError: Document PiP requires user activation`)なので、「ウィンドウ最小化を合図に自動で出す」は作れない=ボタンだけが入口。あわせて**Media Session**にも曲名と再生/停止/曲送りを登録するので、Windowsのメディアパネルとメディアキーからも操作できる(Chromeのタスクバーサムネイルには操作ボタンは出ない=OS側の仕様)。いずれも`file://`直開きで動作を確認済み
- **音の出口はスマホ/タブレットでは`<audio>`要素経由(`src/audio/stream-player.js` getOutputSink、2026-09-23)**: 全形式のプレイヤーと MML 再生は最終段のマスター音量ノードに集まるので、その接続先を1箇所で切り替える。PC は `audioCtx.destination` に直結のまま(遅延を足さない)。iOS/iPadOS の Safari は Web Audio だけの音を他アプリへの切替や画面ロックで止めるが、MediaStreamAudioDestinationNode → `<audio>` で鳴らすと「メディア再生」扱いになって止まらない(iPadOS 26.6.2 で実測。ScriptProcessorNode のままで裏画面/ロック画面とも継続)。Android は直結でも止まらないが、ロック画面のメディア操作(Media Session)が確実になるので同じく `<audio>` 経由にする。`<audio>.play()` はユーザー操作の中でしか呼べないので、document のキャプチャ段で click/touchend/keydown/pointerup を拾い、止まっていれば鳴らし直す(AudioContext.resume() を呼ぶ20箇所以上を個別に触らない)。判定は UA(iPhone/iPad/Android)と「Mac を名乗るタッチ端末」(iPadOS の Safari)。`localStorage['mml_audioSink']` を `element` / `direct` にすると端末判定を上書きできる(PC で `<audio>` 経路を試すとき用)
- **オフライン起動(PWA: `manifest.json` + `sw.js` + `src/pwa.js`、2026-09-23)**: http(s) で開いたときだけ `src/pwa.js` が `<link rel="manifest">` を足して `sw.js` を登録する(file:// では `<link rel="manifest">` を置くだけで CORS エラーが出るので静的に書かない。file:// は元々オフライン)。Service Worker は**ネットワーク優先・失敗時キャッシュ**: オンラインの動きは無いときと同じで、機内モード等で取れないときだけキャッシュから返す(キャッシュ優先にすると index.html だけ新しくて JS が古い混在が起きる)。install 時に index.html を取ってその `<script src>` / `<link href>` を全部先読みするので、キャッシュ一覧を手で持たない。アプリの版が上がっても sw.js を変える必要は無く、SW の作りを変えたときだけ `CACHE` 名を上げる。**インストールの勧誘は出さない**(`beforeinstallprompt` を preventDefault。ブラウザのメニューからは入れられる)。アイコンは `icons/icon-512.png` を足した(Edge headless で icon.svg から書き出し)
- **スマホ/タブレット向けの画面の並べ方(`src/ui/mobileShell.js`、2026-09-23)**: `MML.Device.uiMode()` が `mobile`(タッチ端末が既定。`?ui=mobile` / `?ui=desktop` で上書きでき、選んだ方を `localStorage['mml_ui']` に覚える)のとき `<html class="ui-mobile">` が付き、style.css 末尾の規則でフローティングウィンドウを「1画面に1枚、画面いっぱい」にする(位置/大きさのインライン指定とドラッグ/リサイズは CSS で無効化。floatingWindows.js は触らない)。画面上部にはミニ操作窓の中身(`MiniTransport.dock()`。ファイルを開く/⏮▶⏭■/曲名/シークバー/chミュート)を常設し、下にウィンドウ切替のタブバー(ラベルは各ウィンドウの見出し。鍵盤表示→MMLエディタ→残り)を置く。main.js がウィンドウを「表示」にしたらそのタブへ切り替える(インラインの display を MutationObserver で見張る)。再生・曲送り・ミュート・シークの実体はデスクトップと同じ関数。ヘッダの「PC表示」/ツールバー末尾の「スマホ表示」(タッチ端末のPC表示だけに出る)で行き来する
- **ファイル選択の `accept` はタッチ端末では外す(`src/device.js` MML.Device、2026-09-23)**: iOS の Safari は `accept` に拡張子が並ぶと OS の型(UTI)へ引き当て、合わない項目をファイル選択画面でグレーアウトする。`.nsf` `.spc` `.kss` などは iOS に型が無いので全ファイルが選べなくなる(iPadOS 26.6.2 で実測)。index.html に静的に書いた input は読み込み時に `accept` を外し、JS で作る input は `MML.Device.setFileAccept(el, accept)` を通す(PC では従来どおり付く)。端末判定 `MML.Device.isTouch()` は音の出口の判定と共通(UA が iPhone/iPad/iPod/Android、または Mac を名乗る maxTouchPoints>1 の端末=iPadOS Safari)
- **外部テキストエディタとの同期(`src/ui/fileSync.js`、2026-09-12)**: MMLの正本をディスク上の`.mml`に置いたまま、VS Code / サクラエディタ / 秀丸など好きなエディタで書ける。MMLエディタの「開く」(ピッカー)か、`.mml`をウィンドウへドロップすると、そのファイルの`FileSystemFileHandle`を握って同期を始める(ウィンドウ見出しにファイル名のチップが出る)。外部で保存すると約1秒で本文に反映され、カーソル位置・スクロール位置・再生範囲はそのまま(チップの⟳で自動取り込みをOFFにしても、再生ボタンを押した時点では必ず取り込む)。エディタ側にも未保存の変更があるときは勝手に上書きせず「外部の内容を取り込む / エディタの内容で上書き保存」を選ばせる。保存(ボタンまたはCtrl+S)は同期中のファイルへの上書き、Ctrl+Shift+Sは保存先を選び直す。**`file://`直開きでも全部動く**(2026-09-12 Chrome 153/Windows実測: `file://`はsecure context扱いで`showOpenFilePicker`/`getAsFileSystemHandle`/`createWritable`/IndexedDBがすべて使える。遮断されるのは`fetch`/XHRで隣のファイルを読む経路だけ)。ページを再読込するとハンドルの権限は`prompt`に戻るため自動復帰はできず、チップの「再接続」を1回押す必要がある
- **`@DPCM`の`.dmc`は「開き直したら戻る」「保存したら`.mml`の隣に置く」(`src/ui/dpcmStore.js`、2026-09-16)**: 保存形式は変えない(`.mml`はテキストのみ、`.dmc`は同じフォルダ=ppmckの運用。ppmckcは`@DPCM`のファイル名を`fopen`にそのまま渡し、無ければ環境変数`DMC_INCLUDE`のフォルダを探すので、`.mml`と同じフォルダで`ppmckc`を走らせればそのまま通る)。台帳(`main.js dpcmSampleCache`)に入った`.dmc`はファイル名をキーにIndexedDBへ写し、`.mml`を開いたとき・本文に`@DPCM`定義を書き足したときに参照名で引き戻す(同名は後勝ち)。「開く」とドロップは`.mml`と`.dmc`の同時投入に対応し(`.dmc`だけをドロップしても台帳に入る)、台帳に無い`.dmc`はコンパイル警告`⚠ @DPCM<n> の "x.dmc" が読み込まれていない`で赤く出る(以前は黙って無音だった)。保存すると「`.dmc` を MML と同じフォルダへ書き出す」ボタンが出て、`showDirectoryPicker`で選んだフォルダへ参照中の`.dmc`をまとめて書く。一度選んだフォルダは覚えて、次からはそのフォルダに`.mml`が居ることを`isSameEntry`で確かめたうえで黙って書く(再読込後は権限が`prompt`に戻るので、保存のクリックの中で取り直す)。**初回の保存では必ず手動の1クリックが要る**: `showSaveFilePicker`が返すのはファイルのハンドルだけで親フォルダのハンドルは取れず、ピッカーは1クリックに1回しか開けないので、保存ダイアログの直後に続けてフォルダ選択を出すこともできない。そのため(2026-09-21)**MMLエディタ見出し行に`.dmc`の状態バッジ`#mmlDmcLink`**を足した(ログ欄`#mmlOutput`は2行ぶんの高さしか無く、保存直後のボタンがスクロールの外に隠れて「MMLと同じ場所に置かれたはず」と思ったまま`.dmc`がどこにも無い、ということが起きていた)。バッジは手が要るあいだだけ出る2状態で、**未読込**(台帳に無い=鳴らない、赤)は「フォルダから読み込む」、**未保存**(前回の保存でディスクへ書けていない、橙)は「書き出す」。保存直後のログも`.warn`の「⚠ .dmc {n} 本はまだ書き出されていません」に変え、出した直後に`scrollTop`で見せる。`*2mml`変換が出す`.dmc`も台帳に入るだけになり、Downloadsへは落とさない(以前は変換のたびにDownloadsへ散らばり、別の場所に保存した`.mml`を開き直すとEが無音になっていた)。
  - **逆向き(フォルダ→台帳)も用意した(`readBeside`/`pickAndRead`、2026-09-21)**: 台帳は「ファイル名→バイト列」のコピーであってディスク上の`.dmc`への参照ではないため、別PC・サイトデータを消したあと・別オリジン(`file://`と公開URLは別のIndexedDB)では空になる。`.mml`の隣に`.dmc`が居るなら覚えているフォルダ(無ければ選ばせて)から読み戻せる。開いた瞬間に黙って読むことはできない(再読込で権限が`prompt`に戻る)ので、必ずクリックの中から呼ぶ(`fileSync.js`の「再接続」と同じ事情)
  - **DPCMコンバータの💾の保存先も`.mml`の隣を既定にした(2026-09-21)**: `showSaveFilePicker`に`id`も`startIn`も渡していなかったためChromeの既定(ダウンロードフォルダ)が開き、`.dmc`だけMMLと別の場所に散らばっていた。区間が複数ある行は保存ダイアログを連続で出せないので素のダウンロードに落としていたが、`pickAndWrite`でフォルダを1回選んでまとめて書くようにした
  - ついでに`.mml`とは無関係の2件: `.warn`にCSS規則が無く本文と同じ色で出ていた/`display`を持つ`.mml-file-link`が UAの`[hidden]{display:none}`に勝ってしまい、外部ファイル未接続のときも空のチップが見出し行に残っていた
- **MMLエディタのキー操作(`src/ui/editorKeys.js`、2026-09-08)**: Tab=タブ文字挿入(フォーカスを隣のウィンドウへ飛ばさない。複数行選択で一括インデント、Shift+Tab で外す、Esc の直後の Tab だけ素通し=キーボードでフォーカスを外す道)、F5=再生/一時停止(`btnMmlCapture`。エディタにフォーカスがあるときだけ。ブラウザの再読み込みは止める)、Ctrl+S=保存 / Ctrl+Shift+S=名前を付けて保存(ブラウザの「ページを保存」は止める。外部エディタと同期中なら同じファイルへの上書き)
  - 効かせ方は3段: 割当層(`EnvelopeRegistry`/`PitchEnvelopeRegistry`/`NoteEnvelopeRegistry`/`detune.js` が `cmd` を見て登録自体を止める→ヘッダの`@v/@EP/@MP/@EN`定義も消える)→出力層(`mmlEmit.js emitScore` がチャンネルフラグをANDマスク)→譜面整形(`MML.Convert.shapeEvents`、格子量子化時は小節境界も丸めない)
  - `EN` OFF時はアルペジオ統合(`mergeRapidArpeggio`)自体は行い基音1音として出す(1フレーム連打には戻さない。編曲の出発点としては1音の方が読みやすい)。`MP`/`PT` OFFで`EP` ONなら従来のEPテーブルへ落ちる
  - SPCは従来`v`を一切出していなかったため、`@v` OFF時だけボイス音量(VOL L/R絶対値のピーク、0..127→0..15)を`v`で出す(`extractVoiceEvents`の`vol`)
  - **基準ピッチ(2026-09-07)**: 「自動検出(既定)/12平均律固定(A4=440Hz)」+最小偏差(セント、既定5)。ドライバ固有の音程表で曲全体が数十セントずれている曲(例: Gofer no Yabou II)を、A440基準のまま丸めると全音符に無意味な`D<n>`が付く/系統的にずれた音程で鳴る/半音境界付近の音が転ぶ。自動検出は全音符の「最寄り半音からの偏差」の長さ重み付き中央値(`MML.Convert.detectTuning`)を取り、閾値以上なら**その基準で抽出し直して**ヘッダに`#TUNING <cent>`を出す(`MML.Convert.autoTune`が変換本体を2回走らせる。閾値未満の曲は従来と同一出力)。`#TUNING`は`compiler.js`と`ppmckDriver.js`の周波数テーブルを同じ比でずらすので、ブラウザ再生・NSF書き出し・音程検証(`verify.js`)・鍵盤/ロールの音名(`keyboard.js freqToMidi`)が全て一致する。音名は変わらない(キー/トランスポーズとは別)。SPCも他形式と同じ閾値で適用する(2026-09-07の原音推定修正までは推定の偏りが全体ずれに見えたため15セント/四分位範囲15の専用ガードを掛けていたが、修正後は曲内の推定が揃うので撤去)
- **SPC DSP ガウス補間を実機準拠に + 鍵盤表示「BRR/ガウス補間」の図の修正(2026-09-07)**: `src/emulator/spcDsp.js`。ガウス表を σ=0.63 の合成近似(実機との最大差20)から実機ROM表(blargg/snes9x `gauss[512]`)へ差し替え、補間式も実機の演算順(各タップ個別に >>11、3タップ目までで16bit折り返し、クランプ後 &~1)にした(`Emu.gaussInterp`、blargg参照実装と乱数20万件で完全一致)。鍵盤表示の大波形は以前「48出力サンプルを現ブロック内で折り返して並べた線」で、BRR16点と横軸が対応せず(48出力サンプルの元波形上の長さは 48×pitch/4096 でピッチ依存)、ブロック境界の先も実出力と違っていた。`Emu.previewVoiceWave` で「サンプル位置 (i−2)+frac/256」を横軸にした補間曲線(次ブロックを非破壊に先読みして末尾まで描く)+現在ピッチでの実際の出力サンプル(点、PM有効chは変調後を輪郭の点)に改め、実DSPの出力と値・位置とも一致することを確認
- **SPC サンプル原音推定の修正(2026-09-07、「変換が音痴/不協和音に聞こえる」の真因)**: `src/spc2mml/converter.js detectBrrFundamental`。SPCの絶対音程は各BRRサンプルの原音(基本周波数)推定に依存し、外れるとそのサンプルを使うボイスが丸ごと別の音程へ転ぶ(ロール/鍵盤/MMLは同じ推定値を使うので表示は一貫して間違い、ノートを見ても気づけない)。実音声(ボイスをソロ再生)との照合でコーパス標本143曲中61曲・旋律音符の28.6%が半音以上ズレていた。原因は2つ: (1) 解析窓(約2200サンプル)より短いループ波形(単周期のチップ音風サンプル。ループ32〜240)は配列外を読んで推定不能→C5仮定になっていた(FF4 Theme of Love 主旋律が11半音低い等)→ ループ有りはDSPと同じく**ループ区間をタイルした波形**を解析する。(2) 倍音の強い波形でYINの「最初の谷」が第3倍音だった(Corridors of Time 主旋律が18半音上)→ 最初の谷の整数倍に明らかに深い谷があればそちらを基本波にする。修正後は同じ照合で全件一致(±0.05半音)。打楽器の減衰尾ループ(振幅がサンプル全体の1/10未満)は従来どおり全体解析にして `drumSrcns` の自動判定を変えない
- **SPC→MML 底上げ(2026-08-24、他5形式と同水準へ)**: `src/spc2mml/converter.js`
  - N163借用を8ch化(`n163_0..7`、文字P-W)し、`#EX-NAMCO106 <n>`は実際に使うch数(周波数式と必ず一致させる)
  - FDS/N163はsrcnごとにBRR定常部(ループ有りはループ開始から)基本周期1周期を切り出し(`extractCyclePcm`、検出はdetectBrrFundamental流用・振幅正規化)、`@FM<n>`/`@N<n>`をMML本文に定義して`@<n>`で切替(従来は先頭1ボイスの1波形を波形エディタへ流すだけだった)
  - 音量エンベロープ(`@v`)を全借用先(FDS/N163/VRC6/MMC5/SUNSOFT 5B)へ拡大し、ボイス音量(VOL L/R)を曲中最大値=15へ正規化した0..15段階でテーブル値に乗算(絶対値0..127マッピングだとVOLが小さい曲で全chがv1〜2に潰れるため。DPCM割当ボイスは正規化基準から除外)
  - `D<n>`デチューン: 全チャンネル横断の`detectChorusDetune`(KSS方式)
  - 拡張音源の複数同居可(例: FDS+SUNSOFT 5B+N163。従来は最初の1種類だけ)
  - NON(ノイズ有効)ボイスをノイズch借用時にFLGレート→2A03ノイズ周期idxへ対数距離で丸めてノート化
  - 借用先ラベルを実際の固定チャンネル文字(fds=F, vrc6=M-O, n163=P-W, fme7=X-Z, mmc5=a-b)に修正
  - **チャンネル別の変換音量(2026-08-25)**: 借用先の選択に添えた0-100%シークバー(既定100%=そのまま。v15等で頭打ちのため下げる方向のみ)。`channelMap[ch].volPct`で渡し、計算は`MML.Convert.channelVolScale`(`src/convert/options.js`)に一本化——**パート指定・音色指定・音量指定は同じキー名(`type`/`tone`/`volPct`)のまま他フォーマットのチャンネル割当UIへも展開予定の共通規約**。SPCは曲中ピーク正規化後に乗算し`@v`テーブルと`v`の両方へ反映。volPct=0のchは音程検証から除外
  - ヘッドレス: `node tools/headless/convert.js song.nsf --preset plain` / `--cmd D=0,EP=0,NOTE_END=zero`
- **変換結果の音程検証(全 *2MML 共通、`src/convert/verify.js`)**: 変換直後に最終MMLを実コンパイルし、ブラウザ再生と同じ経路(フレーム毎レジスタ→ロール抽出)で「実際に鳴る音の高さ」を復元して変換元イベントと突き合わせる
  - 各音符の内部3点(30/50/70%)をサンプル。素の音符=3点全て同値で期待と違えば不一致、MP/EP付き=中央値が±1半音超で不一致。EN/PT(意図的に音程が動く)・ノイズD・DPCM E・0.1秒未満はスキップ
  - テンポ整数丸め由来の時間ドリフトは全体尺の比率(0.97〜1.03のとき)で補正
  - 結果は変換ステータスに「音程チェック: 一致 (n 音符を検証)」または「⚠ 音程不一致 n 件」+秒/チャンネル/音名リストで表示し、ピアノロールに赤マーカー(塗り=実際に鳴る高さ、破線枠=元の高さ)を重ね描き(`keyboardDisplay.setConversionDiffs`)
  - 検証失敗(コンパイル不能等)でも変換自体は妨げない。ヘッドレスでは `result.pitchCheck` で取得可能
- **チャンネル割当を鍵盤表示へ統合(2026-08-26)**: 変換元ch→NSF側の借用先パートを選ぶUIは、SPC(ボイスモニターのカード内select)とVGM(専用の割当表)にバラバラに実装されていた。語彙と状態を `src/convert/channelPlan.js`(`MML.Convert.ChannelPlan`)へ集約し、UIは**鍵盤表示の行そのもの**に置いた(鳴らして・ミュートして・見ながら決める作業と同じ場所にする)
  - 前提: 鍵盤表示のpart列は元々 `getPartLetter()` のハードコード規則で「この元chがどのパートになるか」を出していた=**既に割当表だった**。既定値はそこから `targetOfLetter()` で逆引きするので、**触らなければ従来の変換結果と完全に同一**
  - part列を押せるチップにし、クリックで1行ぶんのポップオーバー(借用先/音色/変換音量/自動に戻す)。縦置き・多段・別ウィンドウのどのレイアウトでも行の高さ・列幅が変わらない経路
  - 一覧の見出し(part列)のアイコンをトグルすると「借用先/音色」列が展開する(幅は自動で確保)。スキップ行は減光、借用先が重複した行は赤で警告(従来のSPC/VGMのUIには無かった)
  - 見出しの3列をボタンにした(2026-08-26): **part列**=借用先列の表示トグル(「part」の文字はアイコンに置き換え)、**mute列**=全chミュート⇔全解除のトグル、**vol列**=全chの音量を100%に戻す。列幅・並びは従来のまま(見出しの意味そのものがボタンになっている)
  - 割当を変えるとpart列の文字とピアノロールのレーン名が即追従。SPC/VGMのFM・PCM行のように従来パート文字が空欄だった行にも、変換器の既定割当(`MML.VGM2MML.defaultPlan`)由来の文字が出る
  - **全6形式で変更が効く(2026-08-26)**: 借用先への載せ替え(音量尺度の写像・音色/波形の作り直し・借用先ごとの音程補正/EN/EP・レター割当・重複検出)を `src/convert/borrow.js`(`MML.Convert.Borrow`)に共通化し、KSS/GBS/HESの変換器がユーザー指定時だけそこを通るようにした
    - **既定の割当のときは各変換器の従来コードをそのまま通る**(=既定の変換結果はバイト単位で不変。共通層は「既定から変えたとき」専用の道)
    - 音量は `volTableFor()` 1本で「対数→線形」「線形→対数」「レンジ違い(0-15↔0-63のFDS/VRC6のこぎり)」を全部まかなう。@vテーブルは抽出時にレジストリを写像プロキシへ差し替えて同じ表で揃える(定数音量とテーブルがズレないように)
    - 音程補正の方針は変換元ごとに保つ(`detuneMode`: KSS/VGM=`detectChorusDetune` / GBS/HES=`applyPitchDetune`)
    - NSFはネイティブ変換なので「同じ音源の別ch」「2A03パルス↔MMC5パルス(周期式・音量尺度が同一)」「skip」だけに限定(他チップへ移すにはEP/@vが生レジスタ空間で確定する前=抽出前に借用先を決める必要がある)。UIの選択肢もその範囲に絞る
    - 効かない選択肢はUIに出さない(`CAPS`: 音色指定はVGMではVRC7のみ・NSFは無し、変換音量はSPCのみ)
- **借用先の音域外は「鳴らさない+警告」(2026-08-25)**: 各チップの周期レジスタは有限幅なので出せない音がある(2A03パルスは約55Hz未満が不可)。従来はクランプして**別の音程で鳴らして**いたが、作曲側はオクターブを上げて対処したいので、音程は変えずその音だけ無音にし `warnings` を出す(`src/mml/compiler.js` `pitchRangeIssue`)。`errors` と違い再生/NSF書き出しは中止しない。変換時は音程チェック欄に、手書きMMLの再生/書き出し時はその出力欄に表示
- **SPC再生のフレーム内タイミング再現(2026-08-25)**: SPC再生はキャプチャしたDSP書き込みログを再生する方式だが、1フレーム分の書き込みを**フレーム先頭に一括適用**していたため、AKAO系ドライバのように1フレーム内で多重に制御する曲でKON→KOFFが同一サンプルへ潰れて音が丸ごと消え、GAIN連続書き込みによる減衰も崩れていた
  - 対策: `capture()`/`captureAsync()`が書き込みの**フレーム内サンプル位置(`off`)**を記録し、再生側`_applyWritesUpTo()`が同じ位置で適用する。`off`を持たない旧ログは従来どおり即時適用
  - 実測(FF4 Kingdom Baron): CPU実行の正解に対し音量比 0.720→**1.000**、欠落26フレーム→**0**
  - ★NSF/KSSにも同じ「フレーム先頭一括」構造はあるが、**横展開は退行したため撤回した**(2026-08-25)。NSFは`$4011`直書きスピーチ(水戸黄門)がプチプチノイズ化した。RMS一致だけを根拠に改善と判断したのが誤りで、波形相関で見るとテスト側のシミュレーションが実物の再生経路を再現できていなかった(相関≈0)。横展開する場合は**実物のプレイヤークラスを使った波形比較**で検証すること。GBS/HESはスナップショット方式のため元々対象外

